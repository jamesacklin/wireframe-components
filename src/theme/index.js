const Theme = {
  // Redefine HTML color names to preserve fallback if the user doesn't
  // wrap the app with the ThemeProvider
  colors: {
    black: 'rgb(25,25,25)',
    white: 'rgb(250,250,250)',
    gray: 'rgb(100,100,100)',
    blue: "rgb(0,0,255)"
  }
}

export default Theme